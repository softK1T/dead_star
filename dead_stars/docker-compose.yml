services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    volumes:
      - ./iso.csv:/app/iso.csv

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - api
